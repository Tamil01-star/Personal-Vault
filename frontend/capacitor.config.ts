import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tamilselvan.personalvault',
  appName: 'Personal Vault',
  webDir: 'dist'
};

export default config;
